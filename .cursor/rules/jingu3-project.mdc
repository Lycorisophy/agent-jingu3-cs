---
description: jingu3 CS Agent 项目约定（史诗顺序、版本文档、Ollama、单用户）
globs:
alwaysApply: true
---

# jingu3 CS 项目规则

## 阅读顺序

1. [docs/计划/开发路线图.md](docs/计划/开发路线图.md)（**v2.0** 起为版本与里程碑的单一事实来源）
2. [docs/规范/jingu3-开发规范.md](docs/规范/jingu3-开发规范.md) — **§1.1～§1.3、§2** 为仓库强制约束摘要；完整条文以该文件为准
3. 当前迭代版本目录 `docs/v0.x/`（如 `docs/v0.1/`，含 PRD、设计、接口与部署文档）
4. 客户端 UI（前端）：[docs/设计/JinGu3-AI-Agent客户端UI设计规范.md](docs/设计/JinGu3-AI-Agent客户端UI设计规范.md)
5. 设计指南：[docs/设计/](docs/设计/)、[docs/服务架构.md](docs/服务架构.md)

## 史诗交付顺序（不得越序做业务功能）

**勿在本规则中重复维护长列表。** 版本顺序（v0.3 工具 → … → v0.6 记忆 → v0.7 技能扩展 → v0.8 用户）与业务史诗主题对照见 **[jingu3-开发规范.md §2](docs/规范/jingu3-开发规范.md)**，并与路线图 **v2.0** 一致。

## 每版本工序与文档位置

每个版本在实现前按序完成：**PRD（用户故事 + 验收标准）→ 可行性分析 → 概要设计 → 详细设计 → 服务端实现（注释）→ 接口文档 → 前端（若有）→ 后端单测 → 部署文档**。  
过程文档放在 **`docs/{版本号}/`**（如 `docs/v0.1/`）；源代码在 `agent-jingu3-cs-server/` 与客户端工程。Maven **仓库根** `pom.xml` 为父工程，聚合 `agent-jingu3-cs-server`。

## 本地开发 Ollama 默认模型

- 主推理 / 对话：`gnremy/qwen3.5-abliterated:35b-a3b`
- 嵌入：`qwen3-embedding:8b`  
默认服务地址：`http://localhost:11434`（以实际为准）

## 单用户约定

在用户系统史诗（路线图 **v0.8**）交付完成前，逻辑上仅 **用户 ID `001`、用户名 `user`**。详见 [jingu3-开发规范.md §5](docs/规范/jingu3-开发规范.md)。

## 意图路由优先级（建议）

客户端显式参数 > 配置/规则 > 模型分类。

## 协作与合规

- 遵守 [docs/规范/Agent 辅助开发标准作业程序（SOP）.md](docs/规范/Agent%20辅助开发标准作业程序（SOP）.md) 与 [企业使用AI代理进行代码开发的规范_2026Q1_v1_简略版.md](docs/规范/企业使用AI代理进行代码开发的规范_2026Q1_v1_简略版.md) 中的数据与流程红线。
- 回复用户时使用**简体中文**（若用户未指定其他语言）。
- **按计划完成可交付工作时**：在收尾输出 **Conventional Commits 风格** 的 Git 提交说明摘要，规则全文见 [plan-delivery-git-message.mdc](plan-delivery-git-message.mdc)（用户若需全局生效，可将该文件「格式要求」同步到 Cursor **User Rules**）。

## Java 服务端（`agent-jingu3-cs-server`）编码

- **日志**：生产类使用 Lombok `@Slf4j`，禁止 `LoggerFactory.getLogger`。
- **注入**：优先构造器注入，禁止字段 `@Autowired`（测试除外）。
- **分包**：`common.constant` / `common.enums` / `common.api` / `common.exception`；**入参 DTO** → `common.dto` 及 `common.dto.<domain>`；**出参 VO** → `common.vo` 及 `common.vo.<domain>`；**应用服务** → `service.<domain>`（如 `service.chat`、`service.memory`）；**MyBatis Mapper** → `persistence.mapper.<domain>`；**REST 控制器** → `controller`；**全局异常、可注入常量组件** → `component`；领域枚举可留在 `engine` 等包。
- **数据载体**：`common.dto` / `common.vo` / `ApiResult` 等使用 **Lombok + 传统 JavaBean**（如 `@Getter`/`@Setter`），**不使用** `record`。
- **行动模式（对话 API）**：仅允许在 `mode` / `modePlan` 中选取 **ASK、REACT、PLAN_AND_EXECUTE、WORKFLOW、AGENT_TEAM**。**CRON、STATE_TRACKING、HUMAN_IN_LOOP** 不得通过对话接口选取（定时任务走独立模块或技能；状态追踪/人在环为其它模式的辅助）。选用 **WORKFLOW** 或编排中含 **WORKFLOW** 时必须传 **`workflowId`**。
- **配置注入**：所有 `@Value("${...}")` **必须写默认值**（与 `application.yml` 一致），例如 `@Value("${jingu3.user.id:001}")`；默认值中含冒号时用 `\\:` 转义（如 Ollama 模型名）。
- **API**：统一返回 `ApiResult<T>`；错误码用 `ErrorCode`；业务问题抛 `ServiceException`；全局异常由 `ApiExceptionHandler` 转为 `ApiResult`。
- **文案与提示词**：系统/用户提示框架、路由关键词、Stub/校验/日志占位字符串放在 `common.constant`（如 `PromptTemplates`、`PromptFragments`、`EngineMessages`）；各域 REST 固定提示可放 `*ApiMessages`；业务代码禁止内联长魔法字符串（符合 Alibaba / Sonar 重复字面量规约）。

## 代码质量门禁（Alibaba / SonarLint / Qodana）

- **阿里巴巴 Java 手册**：Maven 已绑定 **P3C PMD** 规则集（`mvn verify` 含 `pmd:check`）。
- **潜在缺陷**：同一 `verify` 含 **SpotBugs**（`spotbugs:check`）；与 Sonar 的 Bug 类规则互补。
- **SonarLint**：IDE 中安装 SonarLint，提交前处理阻断/严重告警；与 Maven 静态分析并行，不互相替代。
- **Qodana**：仓库根 [qodana.yaml](qodana.yaml) + JetBrains `qodana-jvm`；本地需 Docker，命令见 [docs/规范/代码质量与扫描.md](docs/规范/代码质量与扫描.md)。

## 实现前自检

- 是否影响意图场景识别或八大行动模式契约？  
- 是否违反 [开发路线图 v2.0](docs/计划/开发路线图.md) 与 [规范 §2](docs/规范/jingu3-开发规范.md) 的交付顺序？  
- 本版本 `docs/v0.x/` 文档是否随功能同步更新？  
- 新增接口是否走 `ApiResult` 与标准错误码？
- 是否在**仓库根**执行过 `mvn clean verify`（或 `mvn -f agent-jingu3-cs-server/pom.xml clean verify`；或至少 `pmd:check` + `spotbugs:check`）且无新增违规？
