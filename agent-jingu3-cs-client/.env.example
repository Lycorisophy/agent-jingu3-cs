# 开发期留空：走 Vite dev server 同源 + proxy `/api` → 后端（见 electron.vite.config.ts）。
# 打包后 Electron 为 file:// 页面，请设为后端根地址，例如：
# VITE_API_BASE=http://127.0.0.1:8080
VITE_API_BASE=

# 仅 electron-vite dev 使用：代理目标（可选）
# VITE_DEV_API_PROXY=http://127.0.0.1:8080
