<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="30">
    <Properties>
        <!-- 企业常用：时间 | 级别 | 线程 | 类简名 | 请求ID | 追踪ID | 消息与异常 -->
        <Property name="CONSOLE_PATTERN">
            %d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%t] %c{1.} [%X{requestId}] [%X{traceId}] - %msg%n%throwable
        </Property>
    </Properties>
    <Appenders>
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${CONSOLE_PATTERN}" charset="UTF-8"/>
        </Console>
        <SpringProfile name="prod">
            <RollingFile name="RollingFile" fileName="logs/jingu3.log"
                         filePattern="logs/jingu3-%d{yyyy-MM-dd}-%i.log.gz">
                <PatternLayout pattern="${CONSOLE_PATTERN}" charset="UTF-8"/>
                <Policies>
                    <TimeBasedTriggeringPolicy interval="1"/>
                    <SizeBasedTriggeringPolicy size="10 MB"/>
                </Policies>
                <DefaultRolloverStrategy max="14"/>
            </RollingFile>
        </SpringProfile>
    </Appenders>
    <Loggers>
        <Root level="info">
            <AppenderRef ref="Console"/>
            <SpringProfile name="prod">
                <AppenderRef ref="RollingFile"/>
            </SpringProfile>
        </Root>
    </Loggers>
</Configuration>
