# 本地联调 MySQL / Redis / Milvus：复制本文件为 application-local.yml（已加入 .gitignore），再填写密码或设置环境变量。
# 启动示例：
#   java -jar app.jar --spring.profiles.active=local
# 或与默认配置叠加：
#   --spring.profiles.active=dev,local
#
# 推荐环境变量（避免明文进仓库）：
#   MYSQL_PASSWORD、REDIS_PASSWORD、MILVUS_PASSWORD（按需）

---
spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/jingu_agent?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASSWORD:}
  flyway:
    enabled: true
    locations: classpath:db/migration

mybatis-plus:
  mapper-locations: classpath*:mapper/**/*.xml
  type-aliases-package: cn.lysoy.jingu3
  configuration:
    map-underscore-to-camel-case: true
    default-enum-type-handler: org.apache.ibatis.type.EnumTypeHandler

# 引入 spring-boot-starter-data-redis 后可取消注释，与下方 jingu3.redis 指向同一实例即可
#spring:
#  data:
#    redis:
#      host: ${REDIS_HOST:127.0.0.1}
#      port: ${REDIS_PORT:6379}
#      password: ${REDIS_PASSWORD:}
#      database: ${REDIS_DB:0}

jingu3:
  # 用户提示词审计：对称密钥 Base64（32 字节），勿提交仓库；可用环境变量覆盖
  # chat:
  #   persist-user-prompt: true
  # crypto:
  #   user-prompt-aes-key-base64: ${USER_PROMPT_AES_KEY_B64:}
  # 需要 Redis 时置为 true（并确保本机 Redis 可连）
  redis:
    enabled: false
    host: ${REDIS_HOST:127.0.0.1}
    port: ${REDIS_PORT:6379}
    password: ${REDIS_PASSWORD:}
    list-ttl-seconds: 60
  # 需要 Milvus + Ollama 嵌入时置为 true；并确保 Ollama 已拉取 embedding 模型
  milvus:
    enabled: false
    collection-name: jingu3_memory
    host: ${MILVUS_HOST:127.0.0.1}
    port: ${MILVUS_PORT:19530}
    user: ${MILVUS_USER:}
    password: ${MILVUS_PASSWORD:}
  memory:
    injection-enabled: false
  # 需要事件全文检索时启用（本机 Elasticsearch 8.x，索引名默认 jingu3-events）
  elasticsearch:
    enabled: false
    host: ${ES_HOST:127.0.0.1}
    port: ${ES_PORT:9200}
    scheme: http
    index-events: jingu3-events
    username: ${ES_USERNAME:}
    password: ${ES_PASSWORD:}

logging:
  level:
    cn.lysoy.jingu3: INFO
