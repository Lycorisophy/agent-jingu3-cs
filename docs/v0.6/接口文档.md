# v0.6 接口文档 — 记忆实验 API

**基路径**：`/api/v1/memory`  
**统一响应**：`ApiResult<T>`（与全局约定一致）  
**开关**：`jingu3.memory.api-enabled`（默认 `true`）；为 `false` 时不暴露本组接口。

## 1. 创建记忆条目

- **POST** `/api/v1/memory/entries`
- **Content-Type**：`application/json`

**请求体**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| userId | string | 是 | 用户 ID；单用户阶段常用 `001` |
| kind | string | 是 | `FACT` 或 `EVENT` |
| summary | string | 否 | 摘要 |
| body | string | 否 | 正文 |
| factTag | string | 否 | 仅 `kind=FACT` 时写入 `fact_metadata.tag` |
| temporalTier | string | 否 | 仅 `kind=FACT`：`PERMANENT` / `LONG_TERM` / `SHORT_TERM`，默认 `SHORT_TERM`；`kind=EVENT` 时不得传 |
| confirmed | boolean | 否 | 仅 `kind=FACT`：为 `true` 时创建即写入 `fact_metadata.confirmed_at` |

**响应**：`ApiResult<MemoryEntryVo>`

**MemoryEntryVo 字段**：`id`、`userId`、`kind`、`summary`、`body`、`factTag`、`factTemporalTier`、`factConfirmedAt`、`createdAt`、`updatedAt`（时间类为 ISO-8601 字符串，以实际序列化为准）。

## 2. 按用户列出记忆条目

- **GET** `/api/v1/memory/entries?userId={userId}`
- **参数**：`userId` 必填

**响应**：`ApiResult<List<MemoryEntryVo>>`；按创建时间倒序，条数上限 `jingu3.memory.max-list-size`。

## 3. 更新记忆条目

- **PUT** `/api/v1/memory/entries/{id}`
- **请求体**：`userId`（必填，须与条目一致）；`summary`、`body`、`kind`、`factTag`、`temporalTier`、`confirmed` **至少填一项**（与「全空」的 PATCH 区分）。
- **FACT 标签**：`factTag` 传入空字符串可清空 `fact_metadata`；不传 `factTag` 则不修改已有标签。
- **FACT 时间维度**：`temporalTier` 可更新为 `PERMANENT` / `LONG_TERM` / `SHORT_TERM`。
- **FACT 确认**：`confirmed` 为 `true` 写入 `confirmed_at`；为 `false` 清空；`null` 不修改。
- **kind 改为 EVENT**：删除对应 `fact_metadata` 行；不得同时携带 `temporalTier` / `confirmed`。
- **响应**：`ApiResult<MemoryEntryVo>`；若启用 Milvus，会删除旧向量并重建（正文为空则仅删向量侧）。

## 4. 事实确认（MVP）

- **POST** `/api/v1/memory/entries/{id}/confirm?userId={userId}`
- **说明**：仅 `kind=FACT`；将 `fact_metadata.confirmed_at` 置为当前 UTC 时刻；若无 `fact_metadata` 行则插入默认 `temporal_tier=SHORT_TERM` 后再确认。
- **响应**：`ApiResult<MemoryEntryVo>`

## 5. 删除记忆条目

- **DELETE** `/api/v1/memory/entries/{id}?userId={userId}`
- **说明**：先删 Milvus 向量（若启用），再删 MySQL 行（`fact_metadata` / `memory_embedding` 随外键级联）。

## 6. OpenAPI（记忆）

仓库根 [docs/v0.5/openapi.yaml](../v0.5/openapi.yaml) 已包含 `/api/v1/memory/entries` 与 `.../confirm` 路径定义；以本文件与源码为补充说明。

## 7. Elasticsearch 事件 API（v0.6-C）

**前置**：`jingu3.elasticsearch.enabled=true` 且集群可达（默认 `http://127.0.0.1:9200`）。

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/v1/events` | 请求体见 `CreateEventDocumentRequest`；未传 `eventId` 时生成 `evt_` 前缀 ID；返回 `ApiResult` 内含 `eventId` |
| GET | `/api/v1/events/search?userId=&q=&size=` | `q` 为检索词，`size` 默认 10、最大 50 |

**降级（依赖不可用）**：Elasticsearch 或索引异常时，`GET .../search` 仍返回 **HTTP 200** 与 **`data` 空数组**（服务端 WARN 日志）；`POST` 写入失败返回 **HTTP 503**，业务码 **`AG_50301`**（`DEPENDENCY_UNAVAILABLE`），提示事件未写入。

详见 [详细设计](./详细设计.md) 第 5.1 节与 [部署文档](./部署文档.md) 验收步骤。

## 8. 修订记录

| 日期 | 说明 |
|------|------|
| 2026-04-13 | 初稿 |
| 2026-04-13 | v0.6-B：PUT/DELETE、与 Milvus 同步说明 |
| 2026-04-13 | v0.6-C：Elasticsearch 事件 POST/GET |
| 2026-04-13 | ES 事件 API：检索降级空列表、写入 503（`AG_50301`） |
| 2026-04-13 | 事实记忆：`temporal_tier`、`confirmed_at`、`POST .../confirm`；创建/更新/VO 字段 |
