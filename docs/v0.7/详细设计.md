# v0.7 详细设计 — 占位与待决项

> **状态**：本文档为 **占位**：与实现同步的契约以 [接口文档.md](./接口文档.md)、[概要设计.md](./概要设计.md) 及源码为准；下列主题需在 **HITL、MinIO、多用户** 等约束评审后扩展为正式条目。

## 1. 待写入正式详细设计的主题

| 主题 | 说明 | 依赖 |
|------|------|------|
| 工具安全分级矩阵 | 低/中/高危与 `toolId`、`hitl_approval` 绑定规则 | `hitl_approval` 表与审批流 |
| 技能包下载 | MinIO（或等价 OSS）桶布局、预签名 TTL、与 `skill.storage_path` 对齐 | 对象存储与密钥配置 |
| 订阅/取消写入 API | `POST/DELETE` `user_skill` 与幂等、与 `users` 鉴权 | v0.8 用户与 JWT 或单用户阶段简化规则 |
| 客户端工具上报 | 与服务端「可用技能」集合对齐 | [skill-system-design.md](../workspace/skill-system-design.md) |

## 2. 与实现一致的参考（免重复维护长表）

- 工作空间：`PathValidator`、`ProcessSandboxExecutor`、`WorkspaceController`。
- 技能：`SkillController`、`UserSkillMapper` 联表 SQL。
- 配置键：`jingu3.workspace.*`、`jingu3.skill.*`（见 `application.yml`）。

## 3. 修订记录

| 日期 | 说明 |
|------|------|
| 2026-04-13 | 占位初稿：列待决项与引用 |
